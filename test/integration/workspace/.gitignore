# Throwaway fixtures written by the lifecycle integration tests at runtime.
lifecycle/

# Throwaway fixtures written by the polyglot integration tests at runtime.
polyglot/

# Throwaway fixtures + .soukformatrc written by the config-options tests at runtime.
config-options/
.soukformatrc

# Throwaway fixtures written by the power-features integration tests at runtime.
power-features/
.soukformatignore

# Throwaway readiness-probe files written by ensureTidyDefaultFormatter.
.tidy-readiness/

# VS Code writes per-workspace state here when the test host updates settings.
.vscode/
